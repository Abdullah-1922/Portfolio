"use client"
import ScrollToTop from "react-scroll-to-top";

const ScrollToTopComponent = () => {
  return <ScrollToTop className="flex justify-center items-center" smooth color="#6f00ff" />;
};

export default ScrollToTopComponent;